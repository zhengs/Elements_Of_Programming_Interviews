#include <queue>
#include <vector>
#include <iostream>
#include <stack>
#include <unordered_map>
#include <unordered_set>
#include <array>
#include <map>
#include <queue>
#include <utility>
#include <list>
using namespace std;

list<list<pair<int, int> > > occurs(int *m, int* s, int i, int len, int x, int y, int xmax, int ymax) {
	if(m[y*xmax + x] != s[i])
		return;
		if()
		pair<int, int> p(x,y);
		list<pair<int, int> > lp;
		lp.push_back(p);
		list<list<pair<int, int> > > llp;
		llp.push_back(lp);
		return llp;
	} else
}

int main() {

}